import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  display: grid;
  place-content: center;
  height: 400px;
`;

const Wallet = () => <Container>Wallet</Container>;

export default Wallet;
